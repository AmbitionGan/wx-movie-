
<view>
  <view class='banner'style='background-image: url({{backgroundImg}});'>
    <swiper indicator-dots="{{indicatorDots}}" autoplay="{{autoplay}}" interval="{{interval}}" duration="{{duration}}" display-multiple-items="{{displayMultipleItems}}" previous-margin="{{previousMargin}}" next-margin="{{nextMargin}}" bindchange="bindchange" circular="{{circular}}" indicator-color="{{indicatorColor}}" indicator-active-color="{{indicatorActiveColor}}">
      <block wx:for="{{imgUrls}}" wx:key>
        <swiper-item>
          <image src="{{item}}" class="slide-image"/>
        </swiper-item>
      </block>
    </swiper>
  </view>
  <view class='part-1'>
    <scroll-view scroll-X>
      <view class="scroll-view-item">
      
      </view>
    </scroll-view>
  </view>
</view>
